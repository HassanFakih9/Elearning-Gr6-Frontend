import React from 'react';

const TeacherDashboard = ({userId}) => {
  return (
    <div>
      <h2>Teacher Dashboard</h2>
      {/* Add content and functionality specific to the teacher dashboard */}
    </div>
  );
};

export default TeacherDashboard;
